# SMILES-of-fatty-acids
A Python program that converts the XYZ structure of an unbranched fatty acid into its corresponding SMILES string. The script determines molecular connectivity directly from atomic coordinates and outputs the SMILES representation starting with the carboxylic acid group.
